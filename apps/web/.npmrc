# openapi-typescript@7.13.0 still declares a TypeScript 5 peer. Its CLI does
# not load the compiler at runtime, so npm must allow the TS 6 API bridge used
# by typescript-eslint alongside the TypeScript 7 native compiler.
legacy-peer-deps=true
